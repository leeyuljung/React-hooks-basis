const defaultState = {
  inputValue: 'Write something...',
  list: [
    'react',
    'vue',
    'angular'
  ]
}

const reducer = ( state = defaultState, action ) => {
  return state;
}

export default reducer;